// 用户相关store

import { http } from '../../app' // eslint-disable-line

// 定义action type
// export const LOGIN_POST = 'LOGIN_POST'

// 指定对应api
export const api = { }

// 初始化store对象
const state = {}

// 异步操作放到action handler里
const actions = {}

// 根据commit更新store
const mutations = {}


const getters = {}

// 导出vuex模块
export default {
  // 模块开启命名空间
  namespaced: true,
  state,
  actions,
  mutations,
  getters,
}
